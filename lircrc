begin
prog = irexec
button = KEY_PLAY
config = /usr/local/bin/volumio volume mute
end
begin
prog = irexec
button = KEY_RIGHT
config = /usr/local/bin/volumio next
end
begin
prog = irexec
button = KEY_LEFT
config = /usr/local/bin/volumio previous
end
begin
prog = irexec
button = KEY_UP
# for continuous holding button
# repeat = 1
# delay = 2
# config = /usr/local/bin/volumio volume plus
config = /usr/local/bin/volumio volume plus; /usr/local/bin/volumio volume plus; /usr/local/bin/volumio volume plus; /usr/local/bin/volumio volume plus; /usr/local/bin/volumio volume plus
end
begin
prog = irexec
button = KEY_DOWN
# for continuous holding button
# repeat = 1
# delay = 2
# config = /usr/local/bin/volumio volume minus
config = /usr/local/bin/volumio volume minus; /usr/local/bin/volumio volume minus; /usr/local/bin/volumio volume minus; /usr/local/bin/volumio volume minus; /usr/local/bin/volumio volume minus
end
begin
prog = irexec
button = KEY_MENU
config = if [ -f /tmp/remote_lock ]; then rm /tmp/remote_lock; else touch /tmp/remote_lock; fi
end
